apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-k8s-cicd.fullname" . }}-test-connection"
  labels:
    {{- include "demo-k8s-cicd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-k8s-cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
